# Production domain, used by lib/site-config.ts for metadataBase, JSON-LD,
# app/robots.ts and app/sitemap.ts. Must include the protocol, no trailing
# slash. Falls back to a placeholder domain if unset.
NEXT_PUBLIC_SITE_URL=https://likochuang.com
